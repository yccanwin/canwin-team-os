# 3.0 → 4.0 一次性搬家器

此目录只承载离线、一次性、可审计的数据搬家程序，不进入 4.0 日常运行路径。

当前地基只冻结包清单和拒绝危险输入：

- 输入必须是离线加密签名快照；
- 禁止保存 3.0 URL、JWT、数据库密码或 service-role；
- 每个目标项目只允许正式执行一次；
- 每条来源记录必须归入搬入、合并、归档、丢弃或拒绝之一；
- 正式执行器和各业务映射将在对应阶段逐项交付并全量对账。
