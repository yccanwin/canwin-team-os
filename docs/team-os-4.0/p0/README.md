# CanWin Team OS 4.0 — P0 监理台账

> 状态：施工中，G0 未通过
> 基线：`CanWin-Team-OS-4.0-最终施工总方案.md`（2026-07-18）
> 监理分支：`codex/team-os-4-p0-integration`

整体验收进度：**5%**。100% 的口径是 4.0 完整替代 3.0 并通过 G7；下一档 10% 只在 G0 全部要求取得证据后报告。

## 当前结论

P0 已启动，只做现状冻结、安全基线、测试环境和后续接口冻结。P0 未通过前，不开发 P3–P6 业务功能，不修改生产数据库和生产 Storage 策略。

当前已核实：

- 生产项目 `canwinos` 保持不动；独立测试项目 `canwinos-team-os-4-p0-test`（ref `adzerzckgxxibadxkhcr`，东京区）已于 2026-07-19 创建并只读复核为 `ACTIVE_HEALTHY`，月费确认值为 0。
- 生产库有 103 张 `public` 表、162 个 `public` 函数/过程、229 条 `public` 策略、248 个 `public` 索引、11 个 `public` 视图。
- 162 个函数签名的只读元数据和本地调用方候选已登记：148 个 `SECURITY DEFINER`、135 个 authenticated 可执行提权函数、7 个 anon 可执行签名、1 个缺固定 `search_path`；99 个明确 RPC 名称均在线上存在，仍有 49 个 authenticated 可执行签名没有本地直接调用方。该登记不等于权限审计或修复完成。
- 触发器按对象/名称计为 29 个，按 `information_schema.triggers` 事件行计为 46 条；总方案的 46 已确认是事件展开口径。
- 线上和本地迁移版本及名称精确匹配 69/69；本地 69 项版本/文件名/SHA256 门禁已建立，尚未证明远端已执行 SQL 正文与当前文件逐字一致，CI 也未接入。
- 安全顾问当前有 3 个 ERROR、137 个 WARN、3 个 INFO。三个 ERROR 均为安全定义者视图：`finance_public_summary`、`inventory_public_items`、`assets_public`。
- 性能顾问当前有 100 个 WARN、215 个 INFO；包含 205 个未索引外键提示、54 个 RLS 初始化计划提示、46 个重复宽松策略提示、10 个未使用索引提示。
- 309 个外键已精确到源表、字段和目标表；104 个按 Advisor 口径已有覆盖，205 个未覆盖且与 Advisor 集合完全一致。未覆盖候选已分为 P1A 热链137、P1B 其他活动表31、P2 历史观察37；这只是施工顺序候选，尚未创建索引或完成运行时验收。
- Pages 发布工作流候选已增加生产项目 ref、版本化前端 key 指纹和构建产物隔离校验；已新增 PR/手动触发的 P0 本地静态工作流候选，但尚无 GitHub 实际执行证据，也未包含数据库、权限和业务测试。

以上均为只读证据，不代表修复完成，也不代表 G0 通过。

## 七项首批产物

| 编号 | 产物 | 当前状态 | 通过条件 |
| --- | --- | --- | --- |
| 01 | 数据库对象四分类清单 | 表候选/元数据 103/103，函数签名/调用方候选 162/162，外键优先级205/205，验收 0 | 策略/触发器已逐表回连；补真实表级读写入口、函数正文和授权审查、逐索引运行时证据及监理冻结 |
| 02 | 角色权限矩阵 | 规则已冻结，2 人主岗位待确认 | 五主岗位、仓库/主管附加职能和旧角色转换已固化；仍需项目所有者确认 2 人并完成 API 越权测试 |
| 03 | 页面保留/合并/下线清单 | 静态候选完成，验收 0 | 现有真实路由逐项映射，明确重定向或关闭；底层历史数据和附件可恢复 |
| 04 | 图片入口与 Storage 清理清单 | 静态候选完成，验收 0 | 前端入口、数据库记录、桶和策略逐项列出；生产施工期不提前撤权 |
| 05 | 核心实体和状态机字典 | 业务语义已冻结 | 76 条机器断言通过；物理表名/枚举名等待 103 表对象映射 |
| 06 | 测试项目与备份恢复基线 | 独立项目已建，恢复仍阻塞 | 数据库/Auth/Storage 恢复证据、空库预检齐全 |
| 07 | P1 工单及冻结接口 | 机器合同候选，未冻结 | 三团队边界、输入输出、错误语义和禁止事项经监理冻结 |

## G0 门禁

| 要求 | 状态 | 当前证据/缺口 |
| --- | --- | --- |
| 数据库和 Storage 完整备份可用 | 未证明 | 没有本轮可验证的数据库导出与 Storage 对象备份清单 |
| 数据库、Auth、身份岗位关系和 Storage 在独立项目恢复一次 | 未通过 | 独立项目已建，但正式恢复尚未执行 |
| 103 张表四分类 | 进行中 | 候选 47/37/17/2；生产只读元数据 103/103、策略/触发器逐表回连完成；完整17项证据与监理验收仍为0/103 |
| 七项关键业务最小粒度冻结 | 已冻结 | 客户层级、联系人多门店、三价、混合收款、多门店履约、续费和案例粒度已形成 76/76 机器合同；物理表名仍待对象分类映射 |
| 3 个安全 ERROR 有隔离修复证据 | 未通过 | 仅确认错误存在，尚未在隔离项目验证修复 |
| 外键索引和权限风险优先级清单 | 进行中 | 162 个函数完成签名/调用方候选登记但授权验收仍为0；205个未覆盖外键已按137/31/37分级，查询计划、锁和写入影响验收仍为0/205 |
| 测试项目与生产完全隔离 | 进行中 | 独立 ref 已建立并与生产不同；密钥、数据库/Auth/Storage 恢复及运行时隔离尚未验证 |
| 统一自动测试入口与 CI 实际执行 | 未通过 | 提交 `be09535` 的10/10正式本地门禁已通过；本次风险证据候选扩展为十项 static gate，正式结果以新提交的一次性门禁输出为准；GitHub及数据库/权限/业务测试仍无证据 |

## 2026-07-19 本地集成检查点

上一个正式受检源码提交：`be09535`，统一入口10/10、static 9/9、前端构建2027模块、产物106文件全部通过。本次风险证据候选提交后仍只运行一次正式本地门禁；两者都不是 G0、数据库恢复、生产迁移或合并授权。

- 统一入口：10/10；首错即停和 skipped 计数已接入 Windows PR/手动 CI 候选。
- 历史迁移：69/69；历史迁移改动 0。
- 项目 ref 合同：9/9；生产与测试 ref 不同，恢复验证前如实输出 `readiness=BLOCKED reason=isolated-restore-not-validated`。
- 备份恢复合同：549/549；真实恢复状态仍为 `not-run`。
- 前端清单：36 路由、22 个 4.8 节页面、7 个文件入口、7 个 Storage 命名空间。
- P1 导航合同：5 个主岗位、2 个附加职能、13 个应用上下文字段、36 个旧路由映射。
- 后端 catalog 只读校验：9 个自检案例、15 条只读 SQL。
- 三视图候选校验：LF/CRLF/mixed/注释分号 4 种格式通过；4 个正例、5 个负例；3 个视图、3 个调用方；数据库调用 0。
- 103 表四分类：候选 103、验收 0；策略/触发器已逐表回连，真实表级读写和索引风险继续开放，`g0=false`。
- 162 函数候选：签名/元数据 162、调用方交叉核验 162、验收 0；99 个明确 RPC 名称、0 个线上缺失名称、49 个 authenticated 可执行但无本地调用方签名继续待审。
- 309 外键候选：Advisor口径覆盖104、未覆盖205；施工优先级137/31/37，运行时索引决定0/205，生产索引改动0。
- 页面与媒体安全合同：1103 条断言；候选 76、验收 0、孤儿项 0、运行时待验 8。
- 前端构建：2027 个模块成功；生产改动 0。

首次集成检查曾因 Windows CRLF 下未剥离注释分号而把 9 条 SQL 误计为 10 条。检查已按停止线终止；取得新授权后只修改校验器并完成一次新检查。后续 QA merge 冲突同样在首次失败时停止；获授权机械解冲突、完成双组审查后，统一入口只正式运行一次并得到上述结果。

## 停止线

- 不在生产项目执行 DDL、策略、Storage、迁移、恢复或发布操作。
- 不改写现有 69 个迁移，不删除表，不改表名，不批量重写数据。
- 不把页面完成、静态检查、候选库通过、现网兼容、老板验收和允许合并混为一个状态。
- 第一次正式迁移、部署、恢复或发布失败后立即停止，保留证据，等待新决定。
