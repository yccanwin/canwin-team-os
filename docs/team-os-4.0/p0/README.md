# CanWin Team OS 4.0 — P0 监理台账

> **2026-07-22 绿色路线变更令（当前最高优先级）**：4.0 改为全新程序、全新数据库链和独立部署；3.0 永久只读，仅作为数据源、对账源和灾备档案。旧的“在 3.0 上增量升级、继续执行 69/71 条历史迁移、ACL repair、resume/apply、复用当前恢复项目”路线全部关闭。本文后续旧记录只保留为勘察、备份和失败现场证据，不再构成施工授权。
>
> 当前状态：已接受进度仍为 **25%**。既有完整备份、恢复演练、对象清单、岗位规则、业务口径和对账方法继续有效；当前恢复项目只证明 3.0 快照可恢复，不是 4.0 新数据库。全新 4.0 代码根、空白数据库、初始化链、一次性搬家器和六类真实账号页面运行证据尚未完成，因此 G1=false。
>
> 当前下一门：建立 `apps/team-os-4/`、`packages/team-os-4-domain/`、`platform/team-os-4/supabase/`、`tools/migrate-3-to-4/`；锁定 3.0 物理只读；证明新代码不反向依赖旧 `src/` 和旧迁移链；在新空白项目完成初始化、六类账号、页面和直接 API 验收。历史文字中凡出现 `remote/dbPush=true`、`P1 执行已解锁`、`正式恢复1/1仍待执行`、复用/扩展旧表或运行 69/71 条旧迁移，均由本变更令废止。
>
> 基线：`CanWin-Team-OS-4.0-最终施工总方案.md`（2026-07-22）
> 监理分支：`codex/team-os-4-p0-integration`

整体验收进度：**25%**。100% 的口径是 4.0 完整替代 3.0 并通过 G7；下一次只在整体验收结果达到 30% 后报告。

## 当前结论

七项首批产物和 G0 的勘察、备份与规则冻结结果已完成监理验收；P1 历史成功与失败证据均保留，但旧 pre70/post71、ACL 修补和恢复候选已经退出施工路线。当前只允许推进全新 4.0 空白基线和独立搬家器；六类账号页面验收仍未完成，3.0 继续物理只读，当前接受进度25%、G1=false。

路线变更前已核实（以下仅作为历史勘察、恢复与失败现场证据）：

- 生产项目 `canwinos` 保持只读；当前独立测试项目 `canwinos-team-os-4-p0-test-4`（ref `zdmuaqokndhhbarudhtw`，东京区）为 `ACTIVE_HEALTHY`、月费确认值 0，已完成正式封闭恢复 1/1 和全量对账。项目继续封闭，真实账号禁登、预览和外发均未开放。
- 当前成功包为 `D:\CanWin-Team-OS-4.0-Recovery\canwin-team-os-4-p0-20260719T074943659Z-c11fca6bd1`；manifest SHA256 `f4174b91f51f63e37b42e9d907aea0f72aa907ec31694041081ee06c2f6d20b2`，恢复证据 SHA256 `04a6c5d6ac9510747abf5efee27dfe0ecb2a8550191b8ce047b9a1a8d5b458a8`。基础目标指纹与封存生产快照完全相同；最终差异仅为两项已授权岗位覆盖。
- 历史测试项目 `adzerzckgxxibadxkhcr`、`xlpkvjojuqwaqtahaksm`、`gktelqueikmbhtmdbjnh` 均已暂停为 `INACTIVE`，所有失败包和现场不删除、不复用。
- 生产库有 103 张 `public` 表、162 个 `public` 函数/过程、229 条 `public` 策略、248 个 `public` 索引、11 个 `public` 视图。
- 162 个函数已完成逐项分类、正文依赖和当前授权风险审查：102 个现行业务 RPC、17 个兼容 RPC、23 个内部助手、19 个内部触发器、1 个退役候选；7 个函数进入 P0 权限收紧，38 个提权入口进入 P1A 复核。分类完成不等于风险已修复或 G1 六身份运行时通过。
- 触发器按对象/名称计为 29 个，按 `information_schema.triggers` 事件行计为 46 条；总方案的 46 已确认是事件展开口径。
- 线上和本地迁移版本及名称精确匹配 69/69；本地 69 项版本/文件名/SHA256 门禁已建立。GitHub 隔离数据库 CI 候选 16 已通过临时数据库启动、基线、69/69 迁移、26/26 SQL 测试和 4/4 catalog 对账。
- 生产安全顾问基线仍为 3 个 ERROR、137 个 WARN、3 个 INFO，生产未改动。三个安全定义者视图已在当前隔离项目完成一次正式候选、六身份直接 API 验证和顾问复核：候选状态为 0 ERROR / 137 WARN / 3 INFO；随后一次原子回滚成功并清除全部模拟账号/数据，隔离项目恢复到 3 ERROR 基线。
- 性能顾问当前有 100 个 WARN、215 个 INFO；包含 205 个未索引外键提示、54 个 RLS 初始化计划提示、46 个重复宽松策略提示、10 个未使用索引提示。
- 309 个外键已精确到源表、字段和目标表；104 个按 Advisor 口径已有覆盖，205 个未覆盖且与 Advisor 集合完全一致。未覆盖项已冻结为 P1A 热链137、P1B 其他活动表31、P2 历史观察37；这只确定施工顺序，尚未创建索引或完成运行时验收。
- Pages 发布工作流候选已增加生产项目 ref、版本化前端 key 指纹和构建产物隔离校验；P0 工作流包含 Windows 本地集成及 Linux 临时 Supabase/Postgres 两个作业，后者固定执行 1 份基线、69 个迁移、26 个数据库/权限/业务 SQL 测试和 4 项 catalog 对账，不使用仓库密钥，不连接生产。候选 16 的 Windows、Linux 和清理作业均已全绿。
- GitHub 首次正式运行 `29680934378` 中，Windows 本地作业通过；Linux 作业因全栈容器排除名称在 runner 上漂移而停在临时数据库启动步骤，26 个 SQL 测试均未开始。失败运行未重跑，清理步骤成功，生产读写为 0；新候选改用 CLI 原生的仅数据库启动命令，旧失败证据继续保留。
- GitHub 新候选运行 `29681166438` 已证明仅数据库启动、Postgres 17、基线和 69/69 历史迁移全部通过；随后首个 SQL 测试因仍要求 20260713200000 试点启用迁移之前的旧开关状态而失败，后续 25 项未运行。该运行同样未重跑且清理成功，生产读写为 0；候选仅修正测试的最终迁移链口径，不改写任何历史迁移，也不重新开放 3.0 使用。
- GitHub 第三个独立候选运行 `29681350750` 已通过 Windows 门禁、仅数据库启动、基线、69/69 历史迁移及首个 SQL 测试；第二个 `crm_core.sql` 仍要求 20260716114824 联系方式确认后放宽资格规则之前的旧状态而首错停止，后续 24 项未运行。该运行未重跑且隔离数据库清理成功，生产读写为 0；候选只把测试对齐最终迁移链中“联系方式确认是晋级门槛，年费、关键人和 A-D 等级为辅助信息”的既有规则。
- GitHub 第四个独立候选运行 `29681529637` 已越过资格规则断言，但同一 `crm_core.sql` 的精确列清单未包含 20260717184206 已追加且按负责人隔离的 `address` 第 20 列而首错停止。该运行未重跑且隔离数据库清理成功，生产读写为 0；候选冻结最终 20 列顺序、地址隐私和禁止原始电话列三项边界，不改写视图或历史迁移。
- GitHub 第五个独立候选运行 `29681693222` 已通过 Windows 门禁、仅数据库启动、基线、69/69 历史迁移及前两个 SQL 文件；第三个 `customer_import.sql` 在解析 `do$$` 时因 SQL 关键字与美元引用缺少分隔而首错停止。只读横查确认同类词法问题覆盖 14 个测试文件共 17 处；候选机械改为 `do $$`，同时由合同逐个扫描全部 26 个测试文件并拒绝该写法复发，不改业务断言或历史迁移。
- GitHub 第六个独立候选运行 `29681885277` 已证明前两个 SQL 文件继续通过，第三个 `customer_import.sql` 成功解析后把 `RESTRICTIVE FOR ALL` 功能门禁误判成客户端写权限而首错停止。该门禁只会叠加限制、不会授予写入；候选改为同时证明两张导入历史表的限制门禁存在、没有 `PERMISSIVE` 写策略，且 `anon/authenticated` 对 `INSERT/UPDATE/DELETE` 均无有效表权限。运行未重跑、隔离数据库清理成功，生产读写为 0。
- GitHub 第七个独立候选运行 `29682072414` 已证明前三个 SQL 文件通过，第四个 `deal_core.sql` 仍用旧式策略存在性判断，把交易财务历史表的 `RESTRICTIVE FOR ALL` 功能门禁和只读收紧策略误判为可写而首错停止。只读审计确认同类旧断言覆盖 5 个测试文件共 6 处；候选统一改为证明限制门禁存在、没有 `PERMISSIVE` 写策略，且 `anon/authenticated` 对 `INSERT/UPDATE/DELETE` 无有效表权限，并新增静态规则拒绝旧断言复发。旧运行未重跑、隔离数据库清理成功，生产读写为 0。
- GitHub 第八个独立候选运行 `29682375531` 已通过 Windows 门禁、仅数据库启动、基线、69/69 历史迁移和前四个 SQL 文件；第五个 `fulfillment_core.sql` 因函数正文断言要求运算符两侧必须有空格而首错停止，实际函数仍保持“本人或获委托人可读、主管权限不可读”的业务边界。横查确认此类源码格式敏感断言覆盖 6 个测试文件共 15 处；候选统一把函数/视图正文规范化为空白无关、小写后再核验，但通知重试断言当时误按初始迁移变量名修正，未对准最终加固迁移，此遗漏由第九候选继续暴露。旧运行未重跑、隔离数据库清理成功，生产读写为 0。
- GitHub 第九个独立候选运行 `29682720564` 已通过 Windows 门禁、仅数据库启动、基线、69/69 历史迁移及前五个 SQL 文件；第六个 `notification_core.sql` 因测试使用初始迁移的 `attempt_no`，而最终加固迁移已改为 `v_attempt`，首错停止。横查全部 26 个测试读取的定义，冻结 52 个函数/视图对象，其中 28 个在迁移链中被重复定义；新增静态门禁要求每条直接正向源码断言必须存在于该对象最后一次定义所在迁移，并提前发现资格测试仍要求已被最终“联系电话就绪即可转商机”规则替代的旧资格片段。候选把门店区域/品牌一致性继续锁在录入 RPC，将资格转换改核最终联系门禁；旧运行未重跑、隔离数据库清理成功，生产读写为 0。
- GitHub 第十个独立候选运行 `29683060597` 已通过 Windows 门禁、仅数据库启动、基线、69/69 历史迁移及前六个 SQL 文件；第七个 `performance_core.sql` 把最终 `company_profit_summary` 包装视图误当作利润计算正文，因而在视图中查找三类利润账本时首错停止。横查确认同类错位覆盖 `performance_core.sql` 与稍后执行的 `dual_ledger_upgrade.sql` 两份测试；静态门禁已升级为逐对象解析最后一条完整 `CREATE` 语句，并识别引号、注释、嵌套块注释及 dollar-quoted 正文中的分号，杜绝同一迁移文件中计算函数替包装视图“代答”。两份测试现分别核验包装视图调用、安全函数公式和最终原始账本策略缺失；旧运行未重跑、隔离数据库清理成功，生产读写为 0。
- GitHub 第十一个独立候选运行 `29683422624` 已通过 Windows 门禁、仅数据库启动、基线、69/69 历史迁移、全部 7 个数据库测试及前 5 个权限测试；第 13 个 `operations_lead_submission.sql` 仍把 7 参数兼容入口当作完整权限实现，未识别最终迁移已经拆成“7参数 security-invoker 兼容入口 → 8参数 security-definer 权限实现 → 禁止客户端直调的旧算法内核”三层。候选首错停止并清理成功，累计 12/26，生产读写为 0。修正后的测试分别锁定三层职责及调用授权；静态门禁新增函数参数签名、重命名链、声明初始化、后置赋值和 `SELECT ... INTO` 定义来源解析，当前冻结 54 个函数签名/视图对象、其中 28 个重复定义对象，避免同名重载或变量别名再次代答。
- GitHub 第十二个独立候选运行 `29684109383` 已通过 Windows 门禁、仅数据库启动、基线、69/69 历史迁移、全部 7 个数据库测试、全部 10 个权限测试及前 4 个业务测试；第 22 个 `hardware_inventory_behavior.sql` 直接写入最终订单表时漏填 `order_number`，触发最终非空约束而首错停止。真实下单 RPC 会生成订单编号，问题仅在回滚测试夹具；横查发现紧随其后的 `hardware_shipping_chain_behavior.sql` 有同类漏项，两处已一并补齐。静态门禁现冻结 2 个直接订单夹具并强制最终必填编号，候选清理成功，累计 21/26，生产读写为 0，旧运行未重跑。
- GitHub 第十三个独立候选运行 `29684566675` 已通过 Windows 门禁、仅数据库启动、基线、69/69 历史迁移、全部 7 个数据库测试、全部 10 个权限测试及前 8 个业务测试；最后一个 `sales_automation.sql` 因 PostgreSQL 把迁移输入的 `48 hours` 在视图定义中规范化输出为 `48:00:00`，测试按原始文字查找而首错停止。48 小时主管例外规则实际存在，问题仅是等价 interval 写法被误判；候选未重跑、隔离数据库清理成功，累计 25/26，4 项 catalog 对账按首错停止规则未开始，生产读写为 0。修正后的运行断言按规范化输出核验，静态门禁同时把 interval 输入/输出等价形式统一后匹配，避免同类假失败复发。
- GitHub 第十四个独立候选运行 `29685099429` 已证明第十三候选的 interval 修复有效，但同一个最后测试随后暴露第二个旧口径：`sales_automation.sql` 仍按地址迁移前的 19 列核验 `crm_leads_visible`，而最终迁移已追加隐私过滤的 `address` 第 20 列。候选在累计 25/26 时首错停止，4 项 catalog 对账未开始；Windows门禁、临时数据库启动、基线、69/69迁移和前25项测试通过，隔离数据库清理成功，生产读写为0，旧运行未重跑。横查确认精确列清单只存在于 `crm_core.sql` 和 `sales_automation.sql` 两处，前者已正确、后者已对齐；静态门禁现统一解析两种紧凑/换行SQL写法，并要求两处均与最终20列顺序完全一致。
- GitHub 第十五个独立候选运行 `29685639756` 已证明最终20列断言有效，但最后测试随后引用了已被最终管理迁移删除的5参数 `resolve_supervisor_exception`，而当前唯一正式接口是增加 `p_idempotency_key uuid` 的6参数签名。候选在累计25/26时首错停止，4项 catalog 对账未开始；Windows门禁、临时数据库启动、基线、69/69迁移和前25项测试通过，隔离数据库清理成功，生产读写为0，旧运行未重跑。横查确认同一测试共有5处旧签名引用；测试已全部对齐6参数并明确核验幂等键。静态门禁现从基线和69次迁移重建最终162个公开函数身份，逐一核验26份SQL中的189个函数身份引用，同时区分“必须存在”和“明确证明旧接口已删除”两种断言。
- GitHub 第十六个独立候选运行 [`29686358159`](https://github.com/yccanwin/canwin-team-os/actions/runs/29686358159) 首次取得完整全绿证据：Windows 本地总门禁通过；Linux 临时 Postgres 17 启动、基线、69/69迁移、26/26 SQL（数据库7、权限10、业务9）和4/4 catalog对账全部通过；临时数据卷清理成功，仓库密钥、生产读取和生产写入均为0。该候选提交为 `f90fb2ee9dff365a6388049cbe9820e4ac0a771f`，15个失败候选继续原样保留且未重跑。
- 首个 P1 远端候选运行 [`29690060130`](https://github.com/yccanwin/canwin-team-os/actions/runs/29690060130)（提交 `0a8fc72e17ee018638f96c6062cdd9a29362e334`；Windows job `88201083600`，Linux job `88201083572`）已通过 Windows 门禁、基线、70/70 迁移、7/7 数据库测试和前 10 个权限测试；第 18 个 SQL 的 Auth 自动建档夹具重复插入 `profiles`，触发 `profiles_pkey` 首错停止。失败运行未重跑，隔离数据库清理成功，catalog 0/4 未开始，生产读写均为 0；随后由下述新独立候选完成 CI 验收。
- 新独立 P1 候选运行 [`29691027458`](https://github.com/yccanwin/canwin-team-os/actions/runs/29691027458)（提交 `ed853ebbab250f562d03f433f4d2df4ada87de4e`）已全绿：Linux job `88203660504` 用时 2分7秒，基线、70/70 迁移、27/27 SQL（数据库7、权限11、业务9）和 catalog 4/4 全部通过，随后 `supabase stop --no-backup` 清理成功；Windows job `88203660515` 用时 1分37秒，static 15/15、local 12/12、P1 壳层 71/71、1975 模块构建和 66 文件制品均通过。仓库密钥、生产读取和生产写入均为 0。该证据签收 P1 的独立 CI，不代替真实页面和账号验收，因此 G1 仍为 false。
- 独立测试项目 `zdmuaqokndhhbarudhtw` 的唯一正式候选尝试 1/1 已首错停止并保留。dry-run 显示本地70、远端69，仅 P1 迁移 `20260719130910` 待应用；正式应用到第5条 `ALTER profile_access_roles.assignment_kind SET NOT NULL` 时，因 pending trigger events 触发 SQLSTATE `55006`。后续 SQL、catalog 和全量对账均未执行；生产读写0。测试项目发生写入尝试1。失败证据位于 `D:\CanWin-Team-OS-4.0-P1-Validation\p1-isolated-20260719T150630589Z-ed853ebbab\failure.json`，SHA256 `773bf49d6fa8eb3abbe564969cbec83b22755282153fd11e5d5d0fc161cfc996`。其后只读复核确认失败前后13项快照完全一致、69条迁移记录不变、P1对象/夹具/开关残留0、idle transaction 0，事务完整回滚；复核文件 SHA256 `9e77cd23a712b5f908e56af8daf355c81cb36d52f468a10afdb131bea6b74ec3`。这不等于新的正式持久化应用已成功。
- 新修复候选按 `utf8-lf` 口径锁定 6 份候选文件；本机 PostgreSQL 18 针对性回归在 `D:\CanWinP1LocalPgRuns\p1-pending-trigger-iWUhfO` 完成：负控 1/1 复现 SQLSTATE `55006`，修复顺序 4/4、回滚干净、数据库停止、远端连接 0。当前只可记为 `localPostgresAccepted=true`；首次远端失败仍保留，隔离测试项目重新应用仍为 pending，G1 仍为 false。
- 新独立 CI run `29693556452`（HEAD `b9bcca61b826c641e550c6c070f09c4adc407cbe`）已首错停止并保留、不重跑。Linux job `88210359113` 完成迁移70/70、SQL27/27（数据库7、权限11、业务9）、catalog4/4和清理，生产读写0；Windows job `88210359107` 在 local 第一项 static 的第17门失败，前16门已通过，失败仅因 PG selftest 要求 GitHub runner 存在本机 `D:\CanWinP1Postgres18` 三工具，local 其余11项未执行。当前为 `ciRepairCandidateLinuxAccepted=true`、`windowsStatic=16/17`、`portableSelftestRepairPending=true`，不冒充完整 CI 或 G1。
- 第二个 repair CI run `29694104452`（HEAD `92bbac9c265834d0d4f4c550137f519afe366a03`）已首错停止、保留且不重跑。Linux job `88211774885` 完成迁移70/70、SQL27/27（7/11/9）、catalog4/4和清理，生产读写0；Windows job `88211774922` 在 local 第一项 static 的 gate16 `p1-isolated-runtime-runner` 失败，前15门通过，gate17 和 local 其余11项未执行。PG selftest 脚本本身的可移植修复已完成；当前仅可记为 `portableSelftestRepairImplemented=true`、`secondRepairWindowsStatic=15/17`、`validatorLineEndingRepairPending=true`，G1 仍为 false。
- 新独立 CI run [`29694757727`](https://github.com/yccanwin/canwin-team-os/actions/runs/29694757727)（HEAD `8273f5c69e09de24c9afbf27b010d60f7b7caddf`）已双平台全绿，前两次失败继续保留且未重跑。Linux job `88213478676` 用时142秒，迁移70/70、SQL27/27（7/11/9）、catalog4/4并清理；Windows job `88213478682` 用时111秒，static17/17、local12/12、P1壳层71/71、1975模块和66文件静态制品均通过，制品 SHA256 `33505fcddc4b814379906406287b1fa715677b1e218497e1fe5a1693f50fc21b`。GitHub上传制品0；Linux/Windows各1条 Actions Node.js运行时弃用警告及少量依赖弃用提示均不阻断。仓库密钥、生产读取、生产写入均为0。该证据不代替隔离项目持久化应用、全量对账和六类账号页面验收，故 G1/30% 仍为 false。
- Fresh-checkout CI run [`29695919974`](https://github.com/yccanwin/canwin-team-os/actions/runs/29695919974)（HEAD `02f7377071783f2f3213218c6c3c3ace961768bc`）失败后原样保留且未重跑。Windows job `88216547016` 在 static 第6门 `p1-interface-freeze` 首错停止，前5门通过、其余13门及 local其余11项未执行；根因仅为回滚证据文件的 CRLF 原始字节哈希与 UTF-8 LF 合同哈希不一致。Linux job `88216547033` 完成迁移70/70、SQL27/27（7/11/9）、catalog4/4和清理，测试项目远端读写0、生产读写0。机械修复已改为 CRLF/CR→LF 后哈希并覆盖 LF/CRLF/CR/mixed 等价回归；该失败原样保留，由下述新候选独立验证。
- 换行修复后的独立 CI run [`29696529290`](https://github.com/yccanwin/canwin-team-os/actions/runs/29696529290)（HEAD `e04dfa3ee8a9f569b97c905c87f760d7b76a6e00`）已双平台全绿。Linux job `88218121933` 用时132秒，迁移70/70、SQL27/27（7/11/9）、catalog4/4和清理通过；Windows job `88218121940` 用时76秒，static19/19、local12/12、P1壳层71/71和1975模块构建通过，66文件静态制品 SHA256 `33505fcddc4b814379906406287b1fa715677b1e218497e1fe5a1693f50fc21b`，上传制品0。真实账号安全夹具和页面 runner 均只完成无网络自测，页面/账号实际验收仍未完成；仓库密钥、测试项目远端读写和生产读写均为0。
- P1 新候选正式隔离尝试 1/1（run `p1-isolated-20260719T172151689Z-8273f5c69e`）通过迁移阶段70条控制流门后，在首个 SQL `access_control_foundation.sql` 的 `Legacy member received implicit customers.manage permission` 校验误报处首错停止。`preflight.json` SHA256 `e44d53b72c85a71eff2d7a5359220f86c20af56af02a9bf6c0a81716c6d65b97`，`failure.json` SHA256 `3a8077ad58b1a7ee1fc4a75340ab3db9b8f1c3d5ea772e019ff1282136029774`；证据目录 `D:\CanWin-Team-OS-4.0-P1-Validation\p1-isolated-20260719T172151689Z-8273f5c69e`。失败文件没有固化迁移后快照，失败后未重新远端读取，故不宣称当前远端仍为70条。SQL通过0/27、后26项未执行，catalog 0/4、全量对账、最终后快照和六类账号页面验收均未执行；测试项目现场保留，未重试、未清理、未外发，生产读写0。当前为 `post-apply verification candidate pending`，接受进度25%、G1=false。
- 续验资格已由独立 CI run [`29699951990`](https://github.com/yccanwin/canwin-team-os/actions/runs/29699951990) 正式签署：HEAD `a620bb541f4c5eb613413e8b40455b3988ee0cf3`，Linux job `88227205377` 为70/27/7/11/9/4，Windows job `88227205362` 为19/19、12/12。最终 SHA 为 access-control `31fa286b318ad2b24e2d956005c4a5fcc9b0fddfd0269be029330d5c1c3e43f8`、runner `f9d9d6abed29a482757682d25002f2c414a1271e0f7fa2e9360fc62f009ed648`、isolated contract `f99e605341b36e2de18779b6dd52a624b1ef421a9b60c4517f59845a7ba22013`、validator `60a90fc8bf75d44a02c2d824e29b912d14fbbe844af79b0e5a705c77fe59c2af`，断言100/100、fixture pattern4/4。完整对账合同仍为29/27/2/6、key5/raw9/inventory3、内容指纹和两项合法差异。旧apply入口关闭；resume入口仅对签名HEAD之后的干净tracked提交开放，签名HEAD本身拒绝、tracked dirty拒绝、未跟踪 `.codex-audit` 允许；续验远端执行0。资格签署不提高进度，G1=false、25%。
- 正式续验 run `p1-resume-20260719T193911279Z-ea6ed9385d` 已执行1次并首错停止，原现场继续保留。第71迁移 LF SHA256 为 `1bb13f29fc0f5512bd00115dc1c953a2c3aaa0ec21522b1cc8cbb45a18a5cdc0`；notification/P1 access测试SHA分别为 `a3d87069899b986b191bc21826f5e23c65fe4734066e52adc4e14753c9e6e5a3`、`c598b4e4ed3c7e26d9411cb4084685bea1233f47ae969c2685e048f480dac09e`，runtime contract/runner/validator SHA分别为 `4b7db4155637bc349c50948eaa35f2d241e823b3e65c0578e3be083e93344659`、`acae6010b6e4efca94d8411b16aa2cf8e1e04118ecf9055a9711ebb55eb3d2bc`、`1aa9341425465a42ae7cd82d434281595723b00733fafc17be3176f73ee5d5f4`。失败 CI run `29726897764` 原样保留；新 CI run `29733854344`（Linux job `88324427055`、Windows job `88324427244`）双平台成功并签署一次独立测试项目正式资格，G1=false、25%。
- 正式 ACL repair 命令 `p1-acl-repair-20260720T104323349Z-4fa8de78a8` 的解析器失败现场继续保留；其后 run `29738966326` 的双平台成功现在只作为历史解析器修复证据，不得复活旧资格。
- 新正式命令 `p1-acl-repair-20260720T122757275Z-8fa1498850` 在 `db-push-dry-run` 连接独立测试项目直连主机时超时并首错停止。证据目录 `D:\CanWin-Team-OS-4.0-P1-Validation\p1-acl-repair-20260720T122757275Z-8fa1498850` 仅有 `failure.json`，SHA256 `19e4cd30c3d024a452b74f94380a17175364326dc59d41b837bc338c398579ba`；初始light/full迁移均70，完整对账1、私有定义1、Storage归档1，正式尝试0、db push尝试0、持久写0、密钥打印/落盘0、生产读写0，目标保留、未重试、未清理。runner 已让 dry/apply 共用 passwordless Session Pooler `--db-url`，密码仅进入子进程 `PGPASSWORD`，禁止 `--linked`、`--password`、直连、带密URL和继承连接秘密；runtime contract/runner/validator LF SHA256 分别为 `e48ddfe92bdb7db9b63937aa34a3933056c0ba83b59fbaf15bfd5f7722e7a9c9`、`a4e1325f6593c7c6a2ce15ec15477cf53b8a681a3c218ae757699ff1ed3829c0`、`99c2fb29440eb9757924f4e840d96652bc6876908e2cab1e1ad6ea93834796dd`，验证器71/71。全新 Session Pooler CI `29750768517` 已在提交 `370a04aa9fddcce9788df33de3f0ae6924bda932` 通过 Linux/Windows 任务 `88380368836`、`88380368845` 并签名，旧CI与失败HEAD仍永久不可复用；仅 ACL repair 的 `remote/dbPush=true`，正式封闭恢复1/1及全量对账仍待执行；生产仍锁死，G1=false、进度25%。

生产侧全程只读；测试侧恢复、对账、三视图隔离修复和对象分类证据已固化。七项首批产物和 G0 已联合签收，P1 执行已解锁。测试项目仍封闭，真实账号禁登、预览和外发均未开放。

## 七项首批产物

| 编号 | 产物 | 当前状态 | 通过条件 |
| --- | --- | --- | --- |
| 01 | 数据库对象四分类清单 | 已验收 | 103/103 表和 162/162 函数分类冻结；229 策略、29 触发器、205 外键风险逐项回连；生产写入 0 |
| 02 | 角色权限矩阵 | P0 已验收 | 五主岗位、仓库/主管附加职能、旧角色转换、6 个 RPC、4 组字段白名单和测试身份编号均已冻结；运行态越权结果属于 G1 |
| 03 | 页面保留/合并/下线清单 | P0 处置方案已验收 | 36/36 路由和 22/22 页面项已冻结为保留、合并、重定向或关闭；运行态跳转/403/移动端仍待 P1/G1 |
| 04 | 图片入口与 Storage 清理清单 | P0 清理方案已验收 | 7 个文件入口、7 个旧命名空间和两个最终图片槽已冻结；1 bucket、32 objects 已恢复，生产策略未改 |
| 05 | 核心实体和状态机字典 | P0 已验收 | 业务语义 76/76；22 个实体、7 组现有表扩展、11 张新增表、16 组字典、12 条不变量及 RLS/RPC/索引边界均已冻结 |
| 06 | 测试项目与备份恢复基线 | 已验收 | 正式封闭恢复 1/1、数据库/Auth/Storage 全量对账、禁登和外发隔离证据齐全 |
| 07 | P1 工单及冻结接口 | Session Pooler CI已签名，正式恢复待执行 | run29726897764失败、run29733854344历史成功、run29738966326解析器修复及两次正式dry-run失败现场均保留且不可复用；run29750768517是唯一当前签名资格。第71迁移仍为ACL目标6/变化4加私有定义变化1，27项SQL和29份完整快照不变。生产禁止；页面验收未完成，接受进度25%、G1=false |

## G0 门禁

| 要求 | 状态 | 当前证据/缺口 |
| --- | --- | --- |
| 数据库和 Storage 完整备份可用 | 已通过 | 21 项加密制品通过 410/410 运行时校验；manifest SHA256 `f4174b91f51f63e37b42e9d907aea0f72aa907ec31694041081ee06c2f6d20b2` |
| 数据库、Auth、身份岗位关系和 Storage 在独立项目恢复一次 | 已通过 | 正式封闭恢复 1/1；103 表、7 用户、7 identities、32 文件、69 迁移和两项岗位覆盖全量对账通过 |
| 103 张表四分类 | 已通过 | 冻结 47/37/17/2；逐表17项证据 103/103、函数分类 162/162、策略/触发器回连和三个零策略 RPC-only 决定均通过 |
| 七项关键业务最小粒度冻结 | 已通过 | 客户层级、联系人多门店、三价、混合收款、多门店履约、续费和案例粒度已形成 76/76 业务合同；103 表映射及 22实体/7扩展/11新表/16字典/12不变量物理合同已冻结 |
| 3 个安全 ERROR 有隔离修复证据 | 已通过隔离验证 | 正式候选 1/1；匿名拒绝 3/3、五岗位跨团队行 0、视图写入拒绝 15/15；Advisor 降为 0 ERROR 且 WARN 未增加；原子回滚 1/1 后模拟账号/数据清零 |
| 外键索引和权限风险优先级清单 | 已通过分类门禁 | 162 个函数风险分为 P0=7、P1A=38、P2=117；205 个未覆盖外键按137/31/37冻结优先级；具体修复仍需新增迁移和运行证据 |
| 测试项目与生产完全隔离 | 已通过恢复范围 | 独立 ref 与生产不同；生产写入 0，测试用户全部禁登，Functions/Secrets/Cron/外发均为 0，预览未开放 |
| 统一自动测试入口与 CI 实际执行 | 已通过 | GitHub P1 候选 `29691027458` 已证明 Windows static 15/15、local 12/12、P1 71/71；Linux 临时数据库基线、70/70 迁移、27/27 SQL 和 4/4 catalog 全绿，清理成功，生产读写均为 0 |

## 2026-07-19 本地集成检查点

当前新项目恢复、对象分类、物理对象、页面/Storage处置及 P1 接口冻结证据由本次台账固化。历史 GitHub 候选的 static 计数保留原始证据；本地统一静态入口现为 19 项，包含隔离运行器合同、PostgreSQL 回归、真实账号夹具和真实页面 runner 四项纯自测，均禁止默认执行真实数据库或远端操作。G0 已签署；该结论允许 P1 施工，但不等于 P1 运行态、G1、生产迁移、发布或合并授权。

- 统一入口：12/12；首错即停和 skipped 计数已接入 Windows PR/手动 CI 候选；隔离连接边界负测不连接数据库。
- 隔离数据库 CI 合同：当前候选为基线1、迁移71、测试27（数据库7、权限11、业务9）、catalog对账4；历史70迁移CI记录继续按原值保留。run `29726897764`、run `29733854344`、run `29738966326` 及两次正式 dry-run 失败现场均锁定为历史证据。当前 Session Pooler CI `29750768517` 已完成双平台签名，ACL repair `remote/dbPush=true`；正式恢复1/1尚未执行，生产资格未开放。
- 历史迁移：69/69；历史迁移改动 0。
- 项目 ref 合同：17/17；生产与测试 ref 不同，输出 `readiness=READY restore=validated preview=disabled`。
- 恢复台账：139/139；真实恢复状态为 `succeeded`、阻塞项 0；封闭恢复合同 73/73，备份模板合同 1113/1113。
- 前端清单：36 路由、22 个 4.8 节页面、7 个文件入口、7 个 Storage 命名空间。
- P1 导航合同：5 个主岗位、2 个附加职能、13 个应用上下文字段、36 个旧路由映射。
- P1 接口合同：6 个物理 RPC、4 组字段白名单、6 类基础身份、16 个附加职能组合、13 个直接 API 攻击用例、13 项三团队工单；仓库职能按最终方案校正为管理员默认、仅可授实施，销售/运营/财务均有拒绝负控。当前pre70/post71候选冻结六函数ACL最终状态（实际变化4）、一个私有函数定义变化、27项逐项、29份完整快照和两项允许差异；全部历史失败/成功证据已锁定，Session Pooler CI run `29750768517` 已双平台签名，旧run不可复用，正式恢复1/1待执行。页面验收仍pending，生产禁止，G1=false、进度25%。
- 后端 catalog 只读校验：9 个自检案例、15 条只读 SQL。
- 三视图候选校验：LF/CRLF/mixed/注释分号 4 种格式通过；4 个正例、6 个负例；3 个视图、4 条策略、3 个调用方；数据库调用 0。
- 103 表四分类：完整17项证据和监理冻结 103/103；精确恢复副本总行数 796，运行时动态表名调用 0。该单项制品保持 `g0=false` 防止独立冒充整体门禁；整体 G0 已由联合签收单确认。
- 4.0 物理对象：复用 103 表；22 个逻辑实体、7 组现有表扩展、11 张加法式新表、16 组状态字典、12 条不变量及 RLS/RPC/索引边界冻结；13/13 负向自测通过，迁移 SQL 尚未开始。
- 162 函数分类：定义指纹一致 162/162，正文依赖在隔离库内分析且正文返回 0；102 业务 RPC、17 兼容 RPC、23 内部助手、19 触发器、1 退役候选。
- 309 外键：Advisor口径覆盖104、未覆盖205；施工优先级137/31/37已冻结，生产索引改动0。
- 页面与媒体安全合同：1121 条断言；P0 处置方案已签收，运行态候选 76、验收 0、孤儿项 0、运行时待验 8。
- 前端构建：P1 CI 共 1975 个模块成功，66 文件制品通过静态校验；生产改动 0。

首次集成检查曾因 Windows CRLF 下未剥离注释分号而把 9 条 SQL 误计为 10 条。检查已按停止线终止；取得新授权后只修改校验器并完成一次新检查。后续 QA merge 冲突同样在首次失败时停止；获授权机械解冲突、完成双组审查后，统一入口只正式运行一次并得到上述结果。

## 停止线

- 不在生产项目执行 DDL、策略、Storage、迁移、恢复或发布操作。
- 不改写现有 69 个迁移，不删除表，不改表名，不批量重写数据。
- 不把页面完成、静态检查、候选库通过、现网兼容、老板验收和允许合并混为一个状态。
- 第一次正式迁移、部署、恢复或发布失败后立即停止，保留证据，等待新决定。
